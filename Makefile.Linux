#
# Scalien Makefile for Linux
#

INCLUDE = \
	-I$(KEYSPACE_DIR)/src \

LDPATH = \

BASE_CFLAGS = -Wall -W -fPIC -rdynamic $(INCLUDE) -DPLATFORM_$(PLATFORM_UCASE) -D__STDC_FORMAT_MACROS
BASE_CXXFLAGS = $(BASE_CFLAGS)
BASE_LDFLAGS = -lpthread -lrt $(LDPATH)
SOLINK = -shared -fPIC -Wl,-soname,$(SONAME)
SOEXT = so
SWIG_LDFLAGS = -shared -fPIC 
BUNDLEEXT = so
