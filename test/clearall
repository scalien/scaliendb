for ((i = 0; i < 3; i++))
do
	rm -f test/control/$i/system
	rm -f test/control/$i/chunk*
	rm -f test/control/$i/log*
	rm -f test/shard/$i/system
	rm -f test/shard/$i/log*
	rm -f test/shard/$i/chunk*
done

